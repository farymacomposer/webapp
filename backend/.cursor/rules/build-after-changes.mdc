---
globs: src/**/*.cs
alwaysApply: false
---

# Имена асинхронных методов

- В асинхронных методах НЕ используй суффикс `Async` (например, `GetUserId`, а не `GetUserIdAsync`).

# Сборка после изменений

- Запускай: `dotnet build`
- Исправляй ошибки уровня `Error`, если они появились после сборки.